- name: Create service
  kubernetes.core.k8s:
    definition:
      apiVersion: v1
      kind: Service
      metadata:
        name: "{{ app-n8n.name }}"
        namespace: "{{ inventory_hostname }}"
      spec:
        selector:
          app: "{{ app-n8n.name }}"
        ports:
          - name: "{{ app-n8n.name }}-svc-port"
            protocol: TCP
            port: 80
            targetPort: http

- name: Create Route
  kubernetes.core.k8s:
    definition:
      apiVersion: route.openshift.io/v1
      kind: Route
      metadata:
        name: "{{ app-n8n.name }}"
        namespace: "{{ inventory_hostname }}"
        labels:
          app: "{{ app-n8n.name }}"
          route: public-cf
      spec:
        host: "{{ subdomain }}{% if env == 'test' %}-test{% endif %}.{{ domain }}"
        to:
          kind: Service
          name: "{{ app-n8n.name }}"
        port:
          targetPort: "{{ app-n8n.name }}-svc-port"
        tls:
          termination: edge
          insecureEdgeTerminationPolicy: Redirect

- name: Create Deployment
  kubernetes.core.k8s:
    definition:
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: "{{ app-n8n.name }}"
        namespace: "{{ inventory_hostname }}"
      spec:
        replicas: 1
        strategy:
          type: RollingUpdate
          rollingUpdate:
            maxUnavailable: 1
        selector:
          matchLabels:
            app: "{{ app-n8n.name }}"
        template:
          metadata:
            labels:
              app: "{{ app-n8n.name }}"
          spec:
            containers:
              - image: "{{ registry }}/n8n:{{ images.n8n.version }}"
                imagePullPolicy: Always
                name: "{{ app-n8n.name }}"
                ports:
                  - containerPort: 5678
                    name: http
                envFrom:
                  - secretRef:
                      name: "{{ app-n8n.name }}-secrets"
                resources:
                  requests:
                    cpu: 50m
                    memory: 128Mi
                  limits:
                    memory: 512Mi
                readinessProbe:
                  tcpSocket:
                    port: 5678
                  initialDelaySeconds: 5
                  timeoutSeconds: 3
                livenessProbe:
                  tcpSocket:
                    port: 5678
                  initialDelaySeconds: 5
                  timeoutSeconds: 3
                volumeMounts:
                  - name: data
                    mountPath: /home/node
            volumes:
              - name: data
                emptyDir: {}
            imagePullSecrets:
              - name: "{{ pull_secret }}"
